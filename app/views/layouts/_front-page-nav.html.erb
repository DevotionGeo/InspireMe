<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
</a>
<div class="nav-collapse">
  <ul class="nav">
    <% if current_user %>
        <% @assets = Asset.gather_current_user_profile(current_user.id) %>
        <%= link_to image_tag(@assets.first.asset.url(:thumb), :class=> 'img-circle'),@assets.first.asset.url(:original) %>
        <%= link_to 'My Home Page',home_path(:id => current_user.displayname) %>
    <% else %>
        <li><%= link_to "Register", signup_path %></li>
        <li><%= link_to "Log in", login_path %></li>
    <% end %>
  </ul>
</div>